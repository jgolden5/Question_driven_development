In bash, dollar dollar represents a process id, but this should be avoided as a way of creating a tmp filename because it will often create a race condition which allows attackers time to intercept said pid, so a better alternative for making a temp file would be mktemp.
